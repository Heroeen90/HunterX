# HunterX Backend Server Environment Variables

# Port to listen on
PORT=5000

# Optional API key for authentication.
# If set, all requests (except /api/healthz) must include:
#   Header: X-API-Key: <your_key>
# Leave empty to disable authentication (not recommended in production).
HUNTERX_API_KEY=

# Node environment
NODE_ENV=production
